package com.example.tbcexercises.model

import com.example.tbcexercises.R

enum class OrderColor(val names: String,val value: Int) {
    BLUE_GREY(names = "Blue Grey", value = R.color.BlueGrey),
    BROWN(names = "Brown", value = R.color.Brown),
    BLACK(names = "Black", value = R.color.black),
    WHITE(names = "White", value = R.color.white)
}